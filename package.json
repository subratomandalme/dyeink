{
  "name": "dyeink",
  "private": true,
  "version": "3.0.0",
  "scripts": {
    "dev": "cd platform && npm run dev",
    "build": "cd platform && npm run build",
    "preview": "cd platform && npm run preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.87.1",
    "@vercel/node": "^5.5.15"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
