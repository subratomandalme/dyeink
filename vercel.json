{
    "buildCommand": "cd platform && npm install && npm run build",
    "outputDirectory": "platform/dist",
    "rewrites": [
        {
            "source": "/((?!api/.*).*)",
            "destination": "/index.html"
        }
    ]
} 
