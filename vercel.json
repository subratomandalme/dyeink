{
    "buildCommand": "cd platform/web && npm install && npm run build",
    "outputDirectory": "platform/web/dist",
    "rewrites": [
        {
            "source": "/((?!api/.*).*)",
            "destination": "/index.html"
        }
    ]
} 
